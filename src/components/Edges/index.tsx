import React from 'react'
import { coolTopologyTypes } from '../../../'
import { primaryColor } from '../../common/constants'
const Edges: React.FC<coolTopologyTypes.IEdgeProps> = props => {
  const {
    target,
    source,
    label
  } = props
  return <path></path>
}

export default Edges